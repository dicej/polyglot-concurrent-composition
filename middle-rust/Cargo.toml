[package]
name = "middle"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
futures = "0.3.33"
wit-bindgen = { version = "0.60.0", features = ["async-spawn"] }
line-count = { path = "../line-count" }

[workspace]
